{
  "name": "wallet-ui",
  "version": "1.0.0",
  "description": "A React-based Wallet UI project",
  "main": "src/index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "lint": "eslint src --ext .js,.jsx",
    "format": "prettier --write \"src/**/*.{js,jsx}\"",
    "deploy": "npm run build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  },
  "keywords": ["react", "wallet", "ui"],
  "author": "Your Name",
  "license": "MIT",
  "homepage": "."
}
{
  "scripts": {
    "generate-favicon": "node scripts/generate-favicon.js",
    // ... other scripts
  },
  "devDependencies": {
    "sharp": "^0.32.0",
    // ... other dependencies
  }
}
